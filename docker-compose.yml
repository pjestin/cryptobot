version: "3"
services:
  adabnb:
    image: pjestin/cryptobot
    volumes:
      - .:/app/cryptobot
    environment:
      - command=bot_loop.py -p adabnb
  dotbnb:
    image: pjestin/cryptobot
    volumes:
      - .:/app/cryptobot
    environment:
      - command=bot_loop.py -p dotbnb
